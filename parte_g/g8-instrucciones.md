# G8 — Ejecución de Tests

Para que cualquier integrante clone el repositorio y ejecute los tests sin configuración adicional:

1. Activar el entorno virtual e instalar dependencias:
   ```bash
   pip install pytest